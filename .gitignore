# Dart / Flutter
.dart_tool/
.packages
build/
pubspec.lock
pubspec_overrides.yaml

# Este paquete es una librería: el lock se resuelve en la app que lo
# consume, no aquí. Fijarlo obligaría a los consumidores a nuestras
# versiones exactas.

# Editores
.idea/
.vscode/
*.iml

# Sistema
.DS_Store
